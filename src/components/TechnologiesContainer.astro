---
import Technology from "./Technology.astro";

interface Props {
	technologies: string[];
	className: string;
}

const { technologies, className } = Astro.props;
---

<div class=`flex flex-wrap gap-3 ${className}`>
	{
		technologies.map(technology => (
			<Technology technology={technology} />
		))
	}
</div>